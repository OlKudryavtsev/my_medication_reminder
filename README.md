# MedKid
## Изменения v15

- В Mini App на вкладке **Сегодня** кнопка **Выпил** больше не отмечает прием мгновенно.
- При нажатии открывается окно выбора фактического времени приема.
- По умолчанию подставляется текущее время нажатия кнопки, но можно выбрать другое время и затем сохранить.

 Bot — v14

Исправление v14: добавлен недостающий импорт `group_key_for_due` и `group_reminder_text` в `app/main.py`.
Это устраняет ошибку фонового планировщика напоминаний:

```text
NameError: name 'group_key_for_due' is not defined
```

# MedKid Bot — Telegram-бот и Mini App для приема лекарств

Проект для Railway: Telegram-бот (`aiogram`) + Mini App/API (`FastAPI`) + PostgreSQL.

## Что есть в версии v3

- стартовое расписание без Фитомуцила;
- роли: `parent`, `child`, `unknown`;
- все функции закрыты для `unknown`-пользователей, кроме `/start`;
- напоминания ребенку и родителям до отметки;
- кнопки в напоминаниях: `Выпил(а)`, `Отложить 30 мин`, `Пропущено`, `Сегодня`;
- исправлена кнопка `Сегодня` в уведомлениях: расписание отправляется напрямую пользователю, который нажал кнопку;
- при отметке `Выпил(а)` можно указать фактическое время;
- фактическое время можно изменить позже в Mini App или через кнопку `Изменить время приема` в боте;
- даты начала и окончания лечения по каждой строке расписания;
- история и статистика по препаратам;
- родительское администрирование расписания в Mini App;
- редактирование текущего расписания в админке;
- нижняя навигация Mini App: `Сегодня`, `Статистика`, `История`, `Админ`;
- светлая/темная тема с переключателем сверху справа;
- адаптированный минималистичный интерфейс для iPhone;
- 200 коротких юмористических сообщений для напоминаний и 200 благодарственных сообщений.

## Переменные окружения Railway

```env
BOT_TOKEN=токен_бота_от_BotFather
DATABASE_URL=${{Postgres.DATABASE_URL}}
APP_BASE_URL=https://ваш-домен.up.railway.app
PARENT_CHAT_IDS=111111111,222222222
CHILD_CHAT_ID=333333333
TIMEZONE=Europe/Berlin
REMINDER_INTERVAL_MINUTES=7
SNOOZE_MINUTES=30
ALLOW_DEV_INITDATA=false
DEFAULT_BREAKFAST_TIME=08:00
DEFAULT_LUNCH_TIME=13:30
DEFAULT_DINNER_TIME=19:30
```

`ALLOW_DEV_INITDATA=false` рекомендуется для Railway. Если нужно тестировать Mini App в обычном браузере, временно поставьте `true`, но для production верните `false`.

## Обновление уже работающего Railway-проекта

1. Распакуйте архив v3.
2. Замените файлы в текущем GitLab/GitHub-проекте.
3. Запушьте изменения:

```bash
git add .
git commit -m "Update mini app UI and schedule editing"
git push
```

4. Railway автоматически пересоберет сервис.
5. Проверьте переменные `SNOOZE_MINUTES` и `ALLOW_DEV_INITDATA`.
6. Если нужно пересоздать стартовое расписание без Фитомуцила, родитель должен выполнить в Telegram:

```text
/seed_schedule
```

Внимание: `/seed_schedule` удаляет текущие лекарства, расписание и события приема, затем создает стартовый набор заново.

## Команды BotFather

Через `/setcommands` можно задать:

```text
start - запуск и регистрация
today - расписание на сегодня
app - открыть мини-приложение
admin - администрирование расписания для родителей
stats - статистика за 30 дней
history - история по препарату
add - добавить прием лекарства
seed_schedule - пересоздать стартовое расписание
help - помощь
```

## Как пользоваться

### Ребенок

- получает напоминания;
- может нажать `Выпил(а)`, выбрать фактическое время, отложить на 30 минут или отметить пропуск;
- видит вкладки `Сегодня`, `Статистика`, `История`;
- не видит вкладку `Админ`.

### Родители

- получают напоминания и уведомления о действиях ребенка;
- могут отмечать прием, пропуск и отложить прием;
- имеют доступ к `Администрированию`;
- могут добавлять, редактировать и отключать строки расписания;
- могут смотреть статистику и историю по каждому препарату.

### Unknown-пользователи

Если пользователь не указан в `CHILD_CHAT_ID` или `PARENT_CHAT_IDS`, ему доступна только команда `/start`. Бот покажет его Telegram ID, который нужно добавить в Railway Variables.

## Администрирование расписания

Откройте Mini App и перейдите во вкладку `Админ` в нижней панели.

Можно:

- добавить препарат;
- указать дозу;
- указать время приема;
- указать комментарий, например `за 30 мин до еды`;
- указать дату начала курса;
- указать дату окончания курса;
- изменить любую существующую строку расписания;
- отключить строку расписания.

При редактировании расписания будущие `pending`-события по этой строке пересоздаются по новому времени и датам. Уже принятые или пропущенные события остаются в истории.

## Фактическое время приема

В Mini App на вкладке `Сегодня`:

- для ожидающего приема выберите время и нажмите `Выпил`;
- для уже принятого приема измените время и нажмите `Изменить время`.

В Telegram после нажатия `Выпил(а)` бот спрашивает время: `Сейчас`, `По расписанию` или `Ввести время`. После отметки появляется кнопка `Изменить время приема`.

## Проверка после деплоя

1. Откройте:

```text
https://ваш-домен.up.railway.app/health
```

Ожидаемый ответ:

```json
{"status":"ok"}
```

2. Напишите боту:

```text
/start
/today
```

3. Откройте Mini App через кнопку меню Telegram или команду:

```text
/app
```

4. Проверьте нижнюю навигацию, темную тему, отметку приема, редактирование времени и раздел `Админ` у родителя.

## Важные замечания

- Бот работает через polling, webhook не нужен.
- Railway должен иметь один app-сервис и отдельный PostgreSQL-сервис.
- Telegram-бот не может сам написать пользователю, пока пользователь первым не отправил `/start`.
- Если родитель не видит `Админ`, проверьте, что его Telegram ID указан в `PARENT_CHAT_IDS`, а сервис был перезапущен после изменения переменных.

## Изменения v4

- На вкладке **Сегодня** действия по приему уплотнены в одну строку: `Выпил/Изменить`, фактическое время, `+30 мин`, `Пропуск`.
- Добавлен фильтр сверху: **Не принято** / **Все** / **Отложено** / **Пропущено**. По умолчанию выбран режим **Не принято**.
- Таблица статистики растянута на всю ширину экрана.
- Таблица истории по препарату растянута на всю ширину экрана.
- Карточки расписания в **Админ** стали компактнее: меньше высота полей и отступов, чтобы на iPhone помещалось больше лекарств.
- Эмоджи в нижнем меню заменены на встроенные SVG-иконки без внешних зависимостей.

### Обновление с v3 на v4

Замените файлы проекта содержимым архива и выполните:

```bash
git add .
git commit -m "Compact mini app UI v4"
git push
```

Новых переменных окружения для v4 не требуется. После деплоя откройте Mini App из Telegram и проверьте вкладки **Сегодня**, **Статистика**, **История**, **Админ**.


## Изменения v5 — правки интерфейса Mini App

- На вкладке **Сегодня** у непринятых приемов теперь только 3 действия: **Выпил**, **+30 мин**, **Пропустил**.
- У принятых приемов отображается статус и фактическое время; изменение времени открывается отдельной кнопкой **Изменить время**.
- У пропущенных приемов отображается только статус пропуска.
- Кнопка **Изменить** сделана синей.
- Вкладка **Админ** переработана:
  - в блоке **Добавить прием** каждая строка содержит один параметр и одно поле;
  - даты начала и окончания находятся в одной строке;
  - в **Текущем расписании** поля по умолчанию только для чтения;
  - кнопка **Изменить** переводит строку в режим редактирования;
  - в режиме редактирования появляются кнопки **Сохранить** и **Отменить**;
  - кнопка **Отключить** переименована в **Удалить**;
  - высота полей выровнена.

Обновление выполняется как обычно:

```bash
git add .
git commit -m "Mini app interface fixes v5"
git push
```

## Изменения v6 — исправление интерфейса по скриншотам

- Убраны дублирующие строки статуса у принятых и пропущенных приемов на вкладке **Сегодня**.
- Для принятых приемов теперь отображается компактная строка: статус, фактическое время и отдельная синяя кнопка **Изменить**.
- В админке read-only поля текущего расписания заменены на компактные блоки, чтобы iOS не растягивал `date/time`-поля и не выталкивал их за границы карточки.
- В форме **Добавить прием** увеличена ширина подписи слева и исправлены сетки дат/времени, чтобы поля не накладывались друг на друга.
- Исправлены CSS-сетки: проценты заменены на `fr`-колонки с `minmax(0, ...)`, чтобы ширина колонок не превышала ширину карточки из-за отступов.
- Поля дат/времени получили `min-width: 0` и `appearance: none`, чтобы корректнее отображаться в Telegram Mini App на iPhone.

Обновление:

```bash
git add .
git commit -m "Fix mini app layout v6"
git push
```

## Изменения v7

- На вкладке «Сегодня» добавлен фильтр «Выпил»: теперь доступны фильтры «Не принято», «Отложено», «Пропущено», «Выпил», «Все».
- Статус «Выпито» переименован в «Принято».
- Для принятых и пропущенных приемов добавлена кнопка «Изменить статус».
- Для принятых приемов кнопка изменения фактического времени называется «Изменить время».
- Исправлена высота пустого поля «Дата окончания» в админке: пустое значение больше не растягивает поле.

## Обновление v10

Изменения интерфейса и логики:

1. В Mini App на вкладке **Сегодня**:
   - кнопка **Изменить время** больше не просит вводить время текстом;
   - открывается окно выбора времени через нативный time-picker iPhone/Telegram;
   - кнопка **Изменить статус** открывает окно выбора статуса: `Не принято`, `Принято`, `Пропущено`.

2. В Mini App на вкладке **Админ**:
   - блок **Добавить прием** переименован в **Добавить лекарство**;
   - добавлено поле **Периодичность**;
   - поддерживаются варианты:
     - 1 раз в день;
     - 2 раза в день;
     - 3 раза в день;
     - 1 раз в неделю;
     - 1 раз в 2 недели;
     - 1 раз в месяц.
   - если выбран вариант `2 раза в день` или `3 раза в день`, появляются отдельные блоки времени и комментария для каждого приема;
   - лекарство, доза, дата начала и дата окончания задаются один раз и применяются ко всем созданным приемам.

3. В БД добавлены поля расписания:
   - `recurrence_type`;
   - `recurrence_interval_days`.

Миграция выполняется автоматически при старте приложения через `ALTER TABLE`, ручные SQL-команды не нужны.

### Как обновить

```bash
git add .
git commit -m "Add time/status pickers and medicine frequency v10"
git push
```

После деплоя Railway полностью закройте Mini App в Telegram и откройте заново через `/app`, чтобы не подтянулся старый JS-кэш.

## v11 fixes

- В окне «Изменить статус» выбранный статус теперь явно подсвечивается; больше не используется невидимый radio-инпут iOS.
- При добавлении лекарства добавлена защита от двойного нажатия и дедупликация одинаковых приемов на фронте и бэкенде.
- Если такой же активный прием уже есть в расписании, повторная запись не создается.


## Версия v12 — групповые напоминания

Если на одно и то же время запланировано несколько лекарств, бот теперь отправляет одно общее напоминание вместо нескольких отдельных сообщений.

Пример: если в 08:00 нужно принять Панкреатин, Баксет и Атоминекс, придет одно сообщение со списком препаратов и групповыми кнопками:

- ✅ Выпили все
- 😴 Отложить все 30 мин
- ⏭️ Пропущено все
- 📋 Сегодня

Групповая отметка применяет выбранное действие ко всем непринятым препаратам с этим временем приема. Если в группе часть лекарств уже была отмечена, действие применится только к оставшимся `pending`-событиям.

## v13: исправление APP_BASE_URL

Если в Railway переменная `APP_BASE_URL` указана без `https://`, Telegram отклоняет WebApp-кнопку с ошибкой `Only HTTPS links are allowed`. Начиная с v13 приложение само нормализует URL:

- `mymedicationreminder-production.up.railway.app` превратится в `https://mymedicationreminder-production.up.railway.app/app`;
- `https://mymedicationreminder-production.up.railway.app` превратится в `https://mymedicationreminder-production.up.railway.app/app`;
- `https://mymedicationreminder-production.up.railway.app/app` останется как есть.

Рекомендуемое значение Railway Variables:

```env
APP_BASE_URL=https://mymedicationreminder-production.up.railway.app
```

Не добавляйте лишний `/app`, хотя v13 умеет корректно обработать и такой вариант.

## v16: профили ребенка и родителей

В версии v16 добавлены профили:

- профиль **Ребенок** — доступен ребенку и родителям;
- личный профиль каждого родителя — доступен только этому родителю;
- уведомления по личному профилю родителя отправляются только этому родителю;
- уведомления по профилю ребенка отправляются ребенку и родителям;
- ребенок может управлять назначениями в своем профиле;
- родитель может переключаться между профилем ребенка и своим личным профилем в Mini App.

После обновления при первом запуске автоматически создаются профили и существующее расписание переносится в профиль ребенка. В Mini App вверху появится переключатель **Профиль**. Если у пользователя только один доступный профиль, переключатель скрывается.

Новых переменных Railway не требуется.

## v17: группировка в Mini App, несколько детей, журнал изменений и подтверждения

В версии v17 добавлено:

1. **Группировка приемов в Mini App по времени и комментарию.**
   Если на одно время запланировано несколько лекарств, на вкладке **Сегодня** они отображаются одним блоком.
   Для группы доступны действия:
   - `Принял все`;
   - `+30 все`;
   - `Пропустить все`.

2. **Групповые действия через Mini App.**
   Групповое действие сохраняется одной операцией и не создает несколько отдельных уведомлений родителю на каждый препарат.

3. **Улучшенный переключатель профилей.**
   Вместо обычного выпадающего списка используются компактные горизонтальные чипы профилей.

4. **Несколько детских профилей.**
   В разделе **Админ → Профили** родитель может:
   - добавить новый детский профиль;
   - переименовать профиль;
   - удалить детский профиль.

5. **Журнал изменений.**
   В разделе **Админ → Журнал изменений** показываются последние действия по выбранному профилю:
   - отметка приема;
   - изменение статуса;
   - изменение фактического времени;
   - добавление/редактирование/удаление расписания;
   - создание/переименование/удаление профиля.

6. **Защита от случайных действий.**
   Для удаления приема, удаления профиля и группового пропуска добавлено подтверждение через модальное окно.

### Обновление до v17

```bash
git add .
git commit -m "Add grouped UI profiles audit confirmations v17"
git push
```

Новых переменных Railway не требуется. После деплоя полностью закройте Mini App в Telegram и откройте заново через `/app`, чтобы обновился JavaScript.


## Версия 18

Добавлено:

1. **Курсы лечения**
   - В Mini App во вкладке `Админ` появился блок `Курсы лечения`.
   - Можно создать курс с датой начала/окончания, врачом и комментарием.
   - При добавлении лекарства можно выбрать курс.
   - Удаление курса отключает назначения, связанные с курсом.

2. **Шаблоны приема относительно еды**
   - В `Добавить лекарство` появился выбор `Шаблон`:
     - фиксированное время;
     - за 30 минут до еды;
     - во время еды;
     - после еды.
   - Для шаблонов относительно еды выбирается завтрак/обед/ужин, а фактическое время рассчитывается автоматически.

3. **Изменение времени еды на сегодня**
   - В `Админ` появился блок `Сегодня время еды`.
   - Можно указать, что сегодня завтрак/обед/ужин был или будет в другое время.
   - Будущие события для назначений, привязанных к еде, будут рассчитываться от этого времени.

4. **Расширенная периодичность**
   - 1 раз в день, 2/3 раза в день;
   - 1 раз в 2/3 дня;
   - 1 раз в неделю / 1 раз в 2 недели / 1 раз в месяц;
   - дни недели;
   - конкретные даты списком.

5. **Автоматические миграции БД**
   - Добавлены таблицы `treatment_courses` и `meal_overrides`.
   - Добавлены поля расписания: `course_id`, `weekdays`, `specific_dates`, `timing_template`, `meal_name`, `meal_offset_minutes`.

Обновление:

```bash
git add .
git commit -m "Add treatment courses meal templates and advanced recurrence v18"
git push
```

После деплоя полностью закрой Mini App в Telegram и открой заново через `/app`.
